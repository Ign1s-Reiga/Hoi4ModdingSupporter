<?xml version="1.0" encoding="utf-8"?>
<Page
  x:Class="Hoi4ModdingSupporter.Views.LocalisationWorkspaceView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:controls="using:Hoi4ModdingSupporter.Controls"
  xmlns:models="using:Hoi4ModdingSupporter.Models"
  mc:Ignorable="d"
>
  <Grid Margin="36" RowSpacing="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel Spacing="4">
      <controls:HeaderText
        TitleText="{x:Bind ViewModel.Project.DisplayName, Mode=OneWay}"
        TextStyle="{StaticResource TitleTextBlockStyle}"
      />
      <TextBlock
        Text="{x:Bind ViewModel.Project.FolderPath, Mode=OneWay}"
        Foreground="{StaticResource TextFillColorSecondaryBrush}"
        TextTrimming="CharacterEllipsis"
      />
    </StackPanel>

    <Grid Grid.Row="1" ColumnSpacing="20">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="320" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <Grid RowSpacing="12">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="8">
          <TextBlock
            VerticalAlignment="Center"
            Text="Localisation"
            Style="{StaticResource BodyStrongTextBlockStyle}"
          />
          <Button
            Content="Refresh"
            Command="{x:Bind ViewModel.RefreshFilesCommand, Mode=OneWay}"
          />
        </StackPanel>

        <ComboBox
          Grid.Row="1"
          IsEnabled="{x:Bind ViewModel.CanChangeSelection, Mode=OneWay}"
          ItemsSource="{x:Bind ViewModel.Languages, Mode=OneWay}"
          PlaceholderText="Language"
          SelectedItem="{x:Bind ViewModel.SelectedLanguage, Mode=TwoWay}"
        />

        <ListView
          Grid.Row="2"
          IsEnabled="{x:Bind ViewModel.CanChangeSelection, Mode=OneWay}"
          ItemsSource="{x:Bind ViewModel.LocalisationFiles, Mode=OneWay}"
          SelectedItem="{x:Bind ViewModel.SelectedFile, Mode=TwoWay}"
        >
          <ListView.ItemTemplate>
            <DataTemplate x:DataType="models:ProjectWorkspaceFile">
              <StackPanel Padding="0,4">
                <TextBlock
                  Text="{x:Bind RelativePath}"
                  TextTrimming="CharacterEllipsis"
                />
                <TextBlock
                  Text="{x:Bind Name}"
                  Foreground="{StaticResource TextFillColorTertiaryBrush}"
                />
              </StackPanel>
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>
      </Grid>

      <Grid Grid.Column="1" RowSpacing="12">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="12">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>

          <StackPanel>
            <TextBlock
              Text="{x:Bind ViewModel.SelectedFileTitle, Mode=OneWay}"
              Style="{StaticResource BodyStrongTextBlockStyle}"
              TextTrimming="CharacterEllipsis"
            />
            <TextBlock
              Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
              Foreground="{StaticResource TextFillColorSecondaryBrush}"
              TextTrimming="CharacterEllipsis"
            />
          </StackPanel>

          <Button
            Grid.Column="1"
            Content="Save"
            Command="{x:Bind ViewModel.SaveSelectedFileCommand, Mode=OneWay}"
          />
        </Grid>

        <Grid Grid.Row="1" Padding="12,0" ColumnSpacing="12">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240" />
            <ColumnDefinition Width="88" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>

          <TextBlock Text="Key" Style="{StaticResource CaptionTextBlockStyle}" />
          <TextBlock Grid.Column="1" Text="Version" Style="{StaticResource CaptionTextBlockStyle}" />
          <TextBlock Grid.Column="2" Text="Translation" Style="{StaticResource CaptionTextBlockStyle}" />
        </Grid>

        <ListView
          Grid.Row="2"
          ItemsSource="{x:Bind ViewModel.Entries, Mode=OneWay}"
          SelectionMode="None"
        >
          <ListView.ItemTemplate>
            <DataTemplate x:DataType="models:LocalisationEntry">
              <Grid Padding="0,4" ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="240" />
                  <ColumnDefinition Width="88" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBox Text="{x:Bind Key, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox
                  Grid.Column="1"
                  Text="{x:Bind Version, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                />
                <TextBox
                  Grid.Column="2"
                  Text="{x:Bind Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                />
              </Grid>
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>
      </Grid>
    </Grid>

    <TextBlock
      Grid.Row="2"
      Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
      Foreground="{StaticResource TextFillColorSecondaryBrush}"
      TextTrimming="CharacterEllipsis"
    />
  </Grid>
</Page>

<?xml version="1.0" encoding="utf-8"?>
<Page
  x:Class="Hoi4ModdingSupporter.Views.HomeView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:local="using:Hoi4ModdingSupporter.Views"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
  xmlns:ui="using:CommunityToolkit.WinUI"
  xmlns:controls="using:Hoi4ModdingSupporter.Controls"
  xmlns:models="using:Hoi4ModdingSupporter.Models"
  mc:Ignorable="d"
>
  <StackPanel Margin="36">
    <controls:HeaderText
      Margin="0,0,0,36"
      TitleText="Home"
      TextStyle="{StaticResource TitleTextBlockStyle}"
    />
    <controls:HeaderText
      Margin="0,0,0,12"
      TitleText="Recent Projects"
      TextStyle="{StaticResource SubtitleTextBlockStyle}"
    />
    <ListView
      Height="240"
      IsItemClickEnabled="True"
      ItemsSource="{x:Bind ViewModel.RecentProjects, Mode=OneWay}"
      ItemClick="OnRecentProjectClicked"
      SelectionMode="None"
    >
      <ListView.ItemTemplate>
        <DataTemplate x:DataType="models:RecentProjectRecord">
          <Border
            Width="280"
            Height="200"
            Margin="0,0,12,0"
            Padding="12"
            Background="{StaticResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{StaticResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="{StaticResource OverlayCornerRadius}"
          >
            <Grid RowSpacing="10">
              <Grid.RowDefinitions>
                <RowDefinition Height="96" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
              </Grid.RowDefinitions>

              <Grid
                Background="{StaticResource LayerFillColorDefaultBrush}"
                CornerRadius="6"
              >
                <FontIcon
                  Glyph="&#xE8B7;"
                  FontSize="28"
                  Foreground="{StaticResource TextFillColorSecondaryBrush}"
                />
              </Grid>

              <TextBlock
                Grid.Row="1"
                Text="{x:Bind DisplayName}"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                TextTrimming="CharacterEllipsis"
              />
              <TextBlock
                Grid.Row="2"
                Text="{x:Bind FolderPath}"
                Foreground="{StaticResource TextFillColorSecondaryBrush}"
                TextTrimming="CharacterEllipsis"
              />
              <TextBlock
                Grid.Row="3"
                Text="{x:Bind LastAccessed}"
                VerticalAlignment="Bottom"
                Foreground="{StaticResource TextFillColorTertiaryBrush}"
              />
            </Grid>
          </Border>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
    <controls:HeaderText
      Margin="0,24,0,12"
      TitleText="Getting Started"
      TextStyle="{StaticResource SubtitleTextBlockStyle}"
    />
    <StackPanel Spacing="8">
      <toolkit:SettingsCard
        IsClickEnabled="True"
        HeaderIcon="NewFolder"
        Header="Create New Project"
      />
      <toolkit:SettingsCard
        IsClickEnabled="True"
        HeaderIcon="{ui:FontIcon Glyph=&#xE838;}"
        Header="Open Project"
        Click="OnOpenProjectClicked"
      />
    </StackPanel>
  </StackPanel>
</Page>
